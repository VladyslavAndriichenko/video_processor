name: video_processor
description: Application for concatinating videos and converting speech to text

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  flutter_ffmpeg: ^0.4.2
  flutter_bloc: ^7.3.3
  bloc_concurrency: ^0.1.0
  permission_handler: ^8.2.6
  shared_preferences: ^2.0.8
  image_picker: ^0.8.4+4
  share: ^2.0.4
#  esys_flutter_share:
#    git:
#      url: https://github.com/aeyrium/esys-flutter-share

  cupertino_icons: ^1.0.4

dependency_overrides:
  path_provider: ^2.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true